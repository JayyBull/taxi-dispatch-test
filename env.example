# Railway will normally provide this automatically when PostgreSQL is linked.
DATABASE_URL=postgresql://user:password@host:5432/database
# Railway injects PORT automatically. Local default is 8765.
# PORT=8765
